---
// The Root Layout with all global rules
import '@/styles/global.css';
import { cn } from '@/lib/utils';

// Inter font, the main font
import '@fontsource/inter/latin-ext-400'; // All text
import '@fontsource/inter/latin-ext-400-italic';
import '@fontsource/inter/latin-ext-600'; // Bold
import '@fontsource/inter/latin-ext-600-italic';

// Lora font, for headings and highlights
import '@fontsource/lora/latin-400-italic'; // paragraph under headings
import '@fontsource/lora/latin-500-italic'; // Sherqo in the nav
import loraLatin500ItalicWoff2 from '@fontsource/lora/files/lora-latin-500-italic.woff2';
import '@fontsource/lora/latin-600'; // Headings

export interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
}

const {
  title = 'sharqawycs — Sharqawy (personal site)',
  description = 'sharqawycs - personal site of Sharqawy, CS student and builder. Projects, notes, and contact.',
  ogImage = 'https://sharq.tech/og-image.png',
} = Astro.props as Props;

{
  /**
   * 
   * 
<!-- 

Class	Gray scale
prose-gray (default)	Gray
prose-slate	Slate
prose-zinc	Zinc
prose-neutral	Neutral
prose-stone	Stone

--- 
Adapting to dark mode
Each default color theme includes a hand-designed dark mode version that you can trigger by adding the prose-invert class:

<article class="prose dark:prose-invert">{{ markdown }}</article>

----
Class	Body font size
prose-sm	0.875rem (14px)
prose-base (default)	1rem (16px)
prose-lg	1.125rem (18px)
prose-xl	1.25rem (20px)
prose-2xl	1.5rem (24px)
---


Element modifiers
Use element modifiers to customize the style of individual elements in your content directly in your HTML:

<article class="prose prose-img:rounded-xl prose-headings:underline prose-a:text-blue-600">
  {{ markdown }}
</article>
This makes it easy to do things like style links to match your brand, add a border radius to images, and tons more.

Here's a complete list of available element modifiers:

Modifier	Target
prose-headings:{utility}	h1, h2, h3, h4, th
prose-hr:{utility}	hr


[[CONSIDER]]
prose-lead:{utility}	[class~="lead"] 
prose-em:{utility}	em
prose-kbd:{utility}	kbd
prose-code:{utility}	code
prose-pre:{utility}	pre
prose-a:{utility}	a
prose-blockquote:{utility}	blockquote

prose-figure:{utility}	figure
prose-img:{utility}	img
prose-video:{utility}	video


prose-h1:{utility}	h1 ok
prose-h2:{utility}	h2
prose-h3:{utility}	h3
prose-h4:{utility}	h4

prose-p:{utility}	p
prose-figcaption:{utility}	figcaption
prose-strong:{utility}	strong



prose-ol:{utility}	ol
prose-ul:{utility}	ul
prose-li:{utility}	li
prose-table:{utility}	table
prose-thead:{utility}	thead
prose-tr:{utility}	tr
prose-th:{utility}	th
prose-td:{utility}	td

When stacking these modifiers with other modifiers like hover, you most likely want the other modifier to come last:

<article class="prose prose-a:text-blue-600 prose-a:hover:text-blue-500">{{ markdown }}</article>
   */
}

/*Prose and overrides */
const classNames = cn(
  'mx-auto flex min-h-screen flex-col max-w-full bg-background', // Layout, bg-background are for safety and extra-scrolling, see global.css for the real bg
  'prose prose-neutral dark:prose-invert prose-base', // Root Prose
  'prose-headings:font-serif prose-headings:font-semibold', // Headings: h1, h2, h3, h4

  'prose-hr:border-border', // Horizontal Rules
  'prose-a:no-underline prose-a:text-primary prose-a:hover:underline' // a element and links
);
---

<!doctype html>
{/* Theme Provider */}
<script is:inline>
  // Default = dark
  if (typeof localStorage !== 'undefined') {
    const saved = localStorage.getItem('theme');
    if (saved) {
      document.documentElement.classList[saved === 'dark' ? 'add' : 'remove']('dark');
    } else {
      document.documentElement.classList.add('dark'); // force default dark
      localStorage.setItem('theme', 'dark');
    }
  }
</script>

<html lang="en">
  <head>
    <meta charset="UTF-8" />

    {/* Font Preloading: This font needs to be preloaded because it lags, the rest of the page is ok because of the animation */}
    <link rel="preload" as="font" type="font/woff2" href={loraLatin500ItalicWoff2} crossorigin="anonymous" />

    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="color-scheme" content="dark light" />

    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="author" content="sharqawycs" />
    <link rel="canonical" href={Astro.url} />

    <!-- Open Graph -->
    <meta property="og:site_name" content="sharqawycs" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.url} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:secure_url" content={ogImage} />
    <meta property="og:image:type" content="image/png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content={title} />

    <!-- Twitter -->
    <meta name="twitter:image" content={ogImage} />
    <meta name="twitter:image:src" content={ogImage} />
    <meta name="twitter:image:alt" content={title} />
    <link rel="image_src" href={ogImage} />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@sharqawycs" />
  </head>

  <body class={classNames}>
    <slot />
  </body>
</html>
