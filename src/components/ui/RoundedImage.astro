---
import { Image } from 'astro:assets';

const { src, alt, width = 736, height, ...rest } = Astro.props;
---

<Image class="rounded-img" src={src} alt={alt} width={width} height={height} {...rest} />
